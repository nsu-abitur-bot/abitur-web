# Запуск проекта

## Docker

```bash
docker-compose up -d --build
```

После запуска, приложение доступно по адресу `http://localhost:3000`.

## Запуск в dev режиме

Установить зависимости

```bash
pnpm i
```

Запустить приложение `http://localhost:7868`:

```bash
pnpm dev
```
